# eks-terraform
This Terraform code creates an EKS cluster on AWS with the required networking, security groups, IAM roles, and worker nodes. It sets up everything needed to run and manage Kubernetes applications on AWS.
